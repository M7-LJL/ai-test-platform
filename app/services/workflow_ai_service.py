from __future__ import annotations

import logging
from typing import Any

from app.services.ai_service import (
    LLM_MODEL,
    build_requirement_analysis,
    can_use_llm,
    openai_client,
    render_requirement_analysis_markdown,
)

logger = logging.getLogger(__name__)

STAGE_TYPES = (
    "outline",
    "analysis",
    "plan",
    "cases",
    "prepare",
    "execute",
    "defects",
    "report",
)

STAGE_LABELS: dict[str, str] = {
    "outline": "测试大纲",
    "analysis": "需求分析",
    "plan": "测试计划",
    "cases": "用例编写",
    "prepare": "测试准备",
    "execute": "测试执行",
    "defects": "缺陷报告",
    "report": "测试报告",
}

STAGE_ORDER: dict[str, int] = {s: i for i, s in enumerate(STAGE_TYPES)}

_OUTLINE_SYSTEM_PROMPT = """\
你是一名资深的测试工程师。请根据用户提供的PRD或测试点，生成一份可直接导入XMind的Markdown大纲。大纲应遵循以下结构：

- 使用 `#` 表示根节点（项目名称）。
- 使用 `##` 表示主要模块。
- 使用 `###` 表示子功能或测试分类。
- 使用 `-` 表示具体的测试点，必要时可缩进表示子场景（正常/异常/边界）。

要求：
- 覆盖所有功能模块和关键测试点。
- 包含非功能测试（性能、兼容性、安全）和风险依赖（若有）。
- 语言简洁，层次清晰。
请输出纯Markdown内容，不要添加额外解释。"""

_ANALYSIS_SYSTEM_PROMPT = """\
你是一名资深测试分析师。你的目标不是写一份轻量摘要，而是输出一份真正能在项目里继续往下用的“测试需求分析”文档。

请严格基于提供的原始需求、测试大纲、来源锚点和补充说明输出，禁止臆造不存在的业务信息。

核心要求：
- 文档风格要接近真实项目中的需求分析，不要写成模板作文。
- 需求来源必须尽量落到原型、批注、字段名、按钮名、接口、状态、规则、页面、弹窗、任务、配置项等真实锚点。
- 先穷尽功能点，再补真实用户场景，不要只写几条泛泛测试点。
- 每个功能点都要尽量写清入口、动作、规则、关键结果、边界或风险。
- 信息不足时直接写“待确认”，不要自己补业务。
- 禁止补写输入中未明确出现的接口名、字段名、表名、事件名、Webhook、任务名、URL、数据库字段或技术实现细节。
- 不要输出空洞句式，例如“保证功能正常”“验证结果正确”“满足业务需求”等。

请按以下结构输出纯 Markdown：

# 测试需求分析 - <需求名>

## 0. 术语与缩写
用表格输出：
| 术语 | 本需求中的含义 | 需求来源 |

## 1. 测试范围
用表格输出：
| 序号 | 功能点 | 需求来源 | 优先级 | 可测性 | 备注 |

## 2. 测试点

### 一、功能点穷尽清单（按维度遍历）
按以下维度分组输出：
- 角色/用户
- 页面/入口
- 操作/动作
- 接口/数据
- 状态/流程
- 规则/约束
- 非功能
- 关联/依赖
- 隐性需求/界面状态

每个维度下用表格输出：
| 功能点 | 测试关注点 | 对应需求 | 真实用户场景 |

### 一（补）、真实用户使用场景补充
重点补充以下真实场景：
- 误操作/反悔
- 重复/连续操作
- 中断与恢复
- 网络与环境
- 输入与粘贴
- 多端与多态
- 用户差异

用表格输出：
| 场景类型 | 测试关注点 | 对应功能 | 真实用户场景 |

## 3. 需求疑义与遗漏
使用 checklist 输出。只保留当前输入无法直接确定、但会影响计划和用例设计的点。

## 4. 需求追溯
用表格输出：
| 功能点/测试点 | 需求来源 |

## 5. 自检
用 checklist 输出，检查是否覆盖：
- 角色/用户
- 页面/入口
- 操作/动作
- 接口/数据
- 状态/流程
- 规则/约束
- 非功能
- 关联/依赖
- 隐性需求/界面状态
- 理解一致性检查

补充约束：
- 表格里的“测试关注点”要写成真实可验证内容，例如字段展示、联动规则、状态变化、错误提示、限值、依赖关系、任务触发条件。
- “对应需求/需求来源”优先引用短锚点，例如 `原型-列表页`、`批注#4`、`字段：reply_delay_seconds`、`接口：POST /api/...`。
- 如果需求中已经明确字段长度、枚举值、状态值、接口参数、数据库字段、调度规则等，优先原样引用。
- 如果输入里没有这些技术锚点，不要自行发明，统一写 `待确认`。
- 不要把“候选测试点”单独拆成散点列表，应该收敛在维度穷尽和真实用户场景表格中。
- 输出必须是纯 Markdown，不要额外解释。"""

_PLAN_SYSTEM_PROMPT = """\
你是一名资深测试负责人。请基于原始需求、测试大纲、人工确认结果和分析结果，输出一份真正可执行的测试计划，而不是泛泛的计划模板。

要求：
- 优先按业务模块和执行优先级组织内容。
- 明确哪些要先测、为什么先测、依赖什么、风险在哪里。
- 如果已有人工确认测试点，优先围绕这些点规划。
- 用词简洁，避免写空泛表述。
- 不要输出“测试目标如下”“风险如下”这种套话，直接给结论。
- 模块名称、风险点、依赖项尽量使用业务语言，不要泛化成“模块A/流程B”。

请按以下结构输出 Markdown：

## 1. 测试范围与目标
- 本轮覆盖范围
- 本轮重点目标
- 本轮暂不覆盖

## 2. 模块优先级与策略
用表格输出：
| 模块 | 重点场景 | 主要测试类型 | 优先级 | 需求来源 |

## 3. 执行顺序建议
用表格输出：
| 顺序 | 模块/功能 | 先测原因 | 前置依赖 | 预期产出 |

## 4. 风险与关注项
用表格输出：
| 模块 | 风险/问题 | 触发条件 | 缓解措施 | 需求来源 |

## 5. 数据、环境与协作准备
- 测试数据
- 环境/账号/权限
- 外部依赖与协作方

## 6. 准入准出建议
- 准入条件
- 准出条件

输出纯 Markdown，不要额外解释。"""

_CASES_SYSTEM_PROMPT = """\
你是一名资深测试工程师。请把输入材料整理成一份能直接落地执行、风格接近真实项目测试用例库的 Markdown 测试用例。

你的目标风格：
- 先按业务模块分组
- 每个模块下再按 `功能测试 / 异常测试 / 边界测试 / 真实用户场景` 分组
- 每条用例都要具体到页面、动作、预期，不要写成空泛测试点
- 每条用例都尽量保留需求来源，优先引用原型、批注、字段、规则、按钮、页面、真实用户场景
- 如果已有人工确认测试点，优先围绕这些点写用例
- 不要照抄分析文档原句，要把它转成真正可执行的测试步骤

强约束：
- 输出必须是纯 Markdown
- 第一行必须是：`# 测试用例 - <需求名>`
- 模块使用 `## <模块名>`
- 分组使用 `### 功能测试` / `### 异常测试` / `### 边界测试` / `### 真实用户场景`
- 每条用例使用下面格式
- 一条用例只描述一个核心场景
- 标题里要包含模块和场景，不要写空泛的“功能验证”“结果正确”
- 前置条件尽量 1 行
- 步骤尽量 2~5 步，直接写操作
- 预期结果必须是页面、字段、提示、状态、数据、联动、按钮行为等可直接判断的结果
- 如果输入中没有足够依据，不要硬凑大批重复用例
- 没有测试数据时，统一写 `测试数据: 无`
- 需求来源尽量写成短锚点，例如：`原型-列表页`、`批注#4`、`真实用户场景-重复操作`、`隐性需求`
- 如果是同一模块下的多条用例，标题命名要保持统一风格，例如：`账号列表-正常展示全部字段`、`账号列表-空列表状态`
- 不要把“验证”“校验”“检查”“正确展示”当作空泛标题前缀，标题要直接落到场景
- 功能测试优先写主流程、字段展示、联动、保存、删除、筛选、跳转
- 异常测试优先写必填校验、接口失败、权限不足、状态不满足、重复点击、弱网/中断
- 边界测试优先写上限、下限、空值、长度、组合条件、时间边界、状态边界
- 真实用户场景优先写误操作、重复操作、中断恢复、输入粘贴、多标签页、多端、网络异常

每条用例格式：
- **TC-001 模块名-场景名**
  - 优先级: P0/P1/P2
  - 前置条件: ...
  - 测试步骤:
    - 1. ...
    - 2. ...
  - 预期结果: ...
  - 类型: 功能/异常/边界
  - 测试数据: ...
  - 需求来源: ...

标题、前置条件、预期结果写法要求：
- 标题尽量像真实项目里的用例标题，例如 `创建账号-大学专用未选学校直接生成`、`AI生成-连续快速点击生成按钮`
- 前置条件要写成“已存在什么数据 / 当前位于什么页面 / 已完成什么选择”
- 预期结果要写成“页面显示什么、按钮是否可点、字段如何联动、是否新增记录、是否提示错误、数据是否保存”
- 如果需求里明确列出了字段名、按钮名、页面名、弹窗名、状态名，优先原样引用

下面是目标风格示例，只学习写法，不要照抄业务内容：

## 账号列表

### 功能测试

- **TC-001 账号列表-正常展示全部字段**
  - 优先级: P1
  - 前置条件: 已存在至少1条账号记录
  - 测试步骤:
    - 1. 进入账号列表页
    - 2. 查看列表展示
  - 预期结果: 列表正确展示需求中定义的全部字段，操作按钮可见
  - 类型: 功能
  - 测试数据: 无
  - 需求来源: 原型-列表页

### 异常测试

- **TC-002 账号列表-空列表状态**
  - 优先级: P2
  - 前置条件: 当前无任何账号记录
  - 测试步骤:
    - 1. 进入账号列表页
  - 预期结果: 页面展示空状态提示，不报错
  - 类型: 异常
  - 测试数据: 无
  - 需求来源: 隐性需求

输出纯 Markdown，不要额外解释。"""

_PREPARE_SYSTEM_PROMPT = """\
你是一名资深测试工程师。请基于测试计划和测试用例，整理一份测试准备清单。

请按以下结构输出 Markdown：

## 1. 环境准备
列出环境、版本、依赖服务、网络条件、开关配置等。

## 2. 账号与权限
用表格列出：
| 账号类型 | 使用场景 | 所需权限 | 备注 |

## 3. 测试数据准备
用表格列出：
| 数据项 | 用途 | 来源/构造方式 | 风险 |

## 4. 工具与辅助能力
列出抓包、日志、Mock、监控、回滚、埋点验证等准备项。

## 5. 前置检查清单
用 checklist 输出执行前必须确认的事项。

## 6. 阻塞项与依赖
列出当前仍需其他团队支持或待确认的事项。

要求：
- 基于输入内容组织，不凭空发明环境信息
- 输出偏执行前准备，不要写成测试报告
- 输出纯 Markdown，不要额外解释"""

_DEFECTS_SYSTEM_PROMPT = """\
你是一名资深测试工程师。请根据用户提供的测试执行结果/问题现象，编写规范的缺陷报告。

每条缺陷必须包含：
- 标题（一句话概括：在什么情况下出现什么问题）
- 严重程度（致命/严重/一般/轻微）
- 模块
- 环境信息
- 前置条件
- 复现步骤（按操作顺序编号）
- 预期结果
- 实际结果
- 附件说明

严重程度参考：
- 致命：系统崩溃、数据丢失、核心功能不可用
- 严重：主要功能异常、无合理替代方案
- 一般：功能异常但有替代方案、影响次要功能
- 轻微：界面、文案、易用性问题

输出格式（Markdown），每条缺陷用 `## 缺陷：[标题]` 分隔。

请输出纯Markdown内容。"""

_EXECUTE_SYSTEM_PROMPT = """\
你是一名资深测试工程师。请基于测试计划、准备情况和测试结果，整理一份测试执行记录。

请按以下结构输出 Markdown：

## 1. 执行范围
说明本次实际执行覆盖的模块、场景和版本信息。

## 2. 执行概况
用表格列出：
| 项目 | 数值/说明 |
包括：计划执行范围、实际执行范围、阻塞项、回归范围、备注。

## 3. 执行结果明细
用表格列出：
| 模块 | 执行结果 | 关键现象 | 关联用例/测试点 | 备注 |

## 4. 失败项与阻塞项
用表格列出：
| 类型 | 描述 | 影响范围 | 是否需提缺陷 | 备注 |

## 5. 后续动作建议
说明建议补测、回归、提单、风险跟踪动作。

要求：
- 基于输入内容组织执行记录
- 不捏造执行数据
- 输出纯 Markdown，不要额外解释"""

_REPORT_SYSTEM_PROMPT = """\
你是一名资深测试工程师。请根据用户提供的测试执行结果与缺陷统计，编写测试报告。

报告必须包含以下章节：
## 1. 报告摘要
版本、测试周期、测试结论（通过/有条件通过/不通过）、摘要说明

## 2. 测试执行概况
| 项目 | 数量 |
用例总数、已执行、通过、失败、通过率

## 3. 缺陷概况
| 严重程度 | 数量 | 已修复 | 遗留 |
致命/严重/一般/轻微的分布

## 4. 测试覆盖
覆盖模块/功能简述

## 5. 遗留风险与建议
遗留问题及影响、发布建议、后续改进建议

数据来源于实际测试结果，不捏造数据。结论明确、有据可依。
请输出纯Markdown内容。"""

_SYSTEM_PROMPTS: dict[str, str] = {
    "outline": _OUTLINE_SYSTEM_PROMPT,
    "analysis": _ANALYSIS_SYSTEM_PROMPT,
    "plan": _PLAN_SYSTEM_PROMPT,
    "cases": _CASES_SYSTEM_PROMPT,
    "prepare": _PREPARE_SYSTEM_PROMPT,
    "execute": _EXECUTE_SYSTEM_PROMPT,
    "defects": _DEFECTS_SYSTEM_PROMPT,
    "report": _REPORT_SYSTEM_PROMPT,
}

_USER_PROMPT_TEMPLATES: dict[str, str] = {
    "outline": "请根据以下需求文档生成测试大纲：\n\n{content}",
    "analysis": "请基于以下材料生成工程化测试需求分析文档。重点是术语、测试范围、按维度穷尽的测试点、真实用户场景、疑义和需求追溯，不要写成轻量摘要：\n\n{content}",
    "plan": "请基于以下材料生成可执行的测试计划。重点是模块优先级、执行顺序、依赖和风险，而不是大而全模板：\n\n{content}",
    "cases": "请基于以下材料生成结构和粒度都更接近真实项目的测试用例。优先按业务模块和场景类型组织，并优先采用人工确认测试点：\n\n{content}",
    "prepare": "请基于以下测试计划/测试用例生成测试准备清单：\n\n{content}",
    "execute": "请基于以下执行信息生成测试执行记录：\n\n{content}",
    "defects": "请根据以下测试结果/问题现象生成缺陷报告：\n\n{content}",
    "report": "请根据以下测试数据生成测试报告：\n\n{content}",
}


def generate_placeholder_content(stage_type: str, content: str) -> dict[str, Any]:
    """不调用 LLM，输出可编辑的规则占位 Markdown。"""
    if stage_type not in STAGE_LABELS:
        raise ValueError(f"Unknown stage type: {stage_type}")
    label = STAGE_LABELS[stage_type]
    snippet = (content.strip() or "（当前阶段输入为空）")[:2000]
    if len(content.strip()) > 2000:
        snippet += "\n\n…（输入已截断，完整内容见上方「输入内容」）"

    if stage_type == "outline":
        body = f"""# 需求占位大纲（请手动补充）

> 本段为**未使用大模型**时自动生成的占位结构，请按实际模块增删。

## 建议模块一
- 正常场景
- 异常/边界场景

## 建议模块二
- …

---

**依据输入摘要：**

```
{snippet}
```
"""
    elif stage_type == "analysis":
        analysis_payload = build_requirement_analysis(content, use_llm=False)
        body = render_requirement_analysis_markdown(analysis_payload)
    elif stage_type == "plan":
        body = f"""# 测试计划占位（请手动补充）

## 1. 测试目标
（待补充：本轮测试范围、目标与不测范围）

## 2. 测试策略
- 功能测试：
- 接口测试：
- 边界/异常测试：
- 回归测试：

## 3. 优先级与阶段安排
| 阶段 | 目标 | 输入 | 输出 | 优先级 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 计划 | 明确范围与策略 | 需求分析 | 测试计划 | P1 | |

## 4. 资源与分工建议
（待补充）

## 5. 风险与缓解措施
| 风险 | 影响 | 触发条件 | 缓解措施 |
| --- | --- | --- | --- |

## 6. 准入准出建议
（待补充）

---

**依据输入摘要：**

```
{snippet}
```
"""
    elif stage_type == "cases":
        body = f"""# 测试用例占位（请手动补充）

## 模块 A

- **TC-001 （示例标题）**
  - 优先级: P2
  - 前置条件: …
  - 测试步骤:
    - 1. …
    - 2. …
  - 预期结果: …
  - 类型: 功能
  - 需求来源: …

---

**依据输入摘要：**

```
{snippet}
```
"""
    elif stage_type == "prepare":
        body = f"""# 测试准备占位（请手动补充）

## 1. 环境准备
- 环境版本：
- 依赖服务：
- 开关配置：

## 2. 账号与权限
| 账号类型 | 使用场景 | 所需权限 | 备注 |
| --- | --- | --- | --- |

## 3. 测试数据准备
| 数据项 | 用途 | 来源/构造方式 | 风险 |
| --- | --- | --- | --- |

## 4. 工具与辅助能力
- 抓包：
- 日志：
- Mock：
- 监控：

## 5. 前置检查清单
- [ ] 环境可访问
- [ ] 账号可用
- [ ] 测试数据准备完成

## 6. 阻塞项与依赖
（待补充）

---

**依据输入摘要：**

```
{snippet}
```
"""
    elif stage_type == "execute":
        body = f"""# 测试执行记录占位（请手动补充）

## 1. 执行范围
（待补充：模块、场景、版本）

## 2. 执行概况
| 项目 | 数值/说明 |
| --- | --- |
| 计划执行范围 | |
| 实际执行范围 | |
| 阻塞项 | |
| 回归范围 | |

## 3. 执行结果明细
| 模块 | 执行结果 | 关键现象 | 关联用例/测试点 | 备注 |
| --- | --- | --- | --- | --- |

## 4. 失败项与阻塞项
| 类型 | 描述 | 影响范围 | 是否需提缺陷 | 备注 |
| --- | --- | --- | --- | --- |

## 5. 后续动作建议
（待补充）

---

**依据输入摘要：**

```
{snippet}
```
"""
    elif stage_type == "defects":
        body = f"""# 缺陷报告占位（请手动补充）

## 缺陷：（示例标题）

- **严重程度**：一般
- **模块**：
- **环境**：
- **前置条件**：
- **复现步骤**：
  1. …
- **预期结果**：
- **实际结果**：

---

**依据输入（执行结果/日志摘要）：**

```
{snippet}
```
"""
    else:  # report
        body = f"""# 测试报告占位（请手动补充）

## 1. 报告摘要
（版本、周期、结论）

## 2. 测试执行概况
| 项目 | 数量 |
| --- | --- |
| 用例总数 | |
| 已执行 | |
| 通过 | |
| 失败 | |

## 3. 缺陷概况
（待填写）

## 4. 测试覆盖
（待填写）

## 5. 遗留风险与建议
（待填写）

---

**依据输入摘要：**

```
{snippet}
```
"""

    return {
        "output": body.strip(),
        "prompt_used": f"[placeholder] stage={stage_type} ({label})，未调用大模型",
    }


def generate_stage_content(stage_type: str, content: str) -> dict[str, Any]:
    if stage_type not in _SYSTEM_PROMPTS:
        raise ValueError(f"Unknown stage type: {stage_type}")

    if not can_use_llm():
        raise RuntimeError("LLM 未配置，请在 .env 中设置 LLM_API_KEY 和 LLM_BASE_URL。")

    system_prompt = _SYSTEM_PROMPTS[stage_type]
    user_prompt = _USER_PROMPT_TEMPLATES[stage_type].format(content=content.strip())

    response = openai_client.chat.completions.create(
        model=LLM_MODEL,
        temperature=0.3,
        messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_prompt},
        ],
    )

    result = response.choices[0].message.content or ""

    return {
        "output": result,
        "prompt_used": f"[system]\n{system_prompt}\n\n[user]\n{user_prompt[:200]}...",
    }
